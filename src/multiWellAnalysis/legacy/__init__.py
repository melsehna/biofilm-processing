"""Quarantined legacy / unsupported modules.

Nothing in the supported pipeline imports this subpackage. These modules are kept
for reference and backward compatibility only — they are divergent, unused, or
contain hardcoded personal paths. Do not extend them; add new work to the
supported paths (see the repo README / CLAUDE.md). See legacy/README.md.
"""
